# Copy to .env (gitignored) and fill in. Keys never go in the repo.

# TypeSafe / Jev judgment API — https://docs.typesafe.ai
TYPESAFE_API_KEY=

# Buffer MCP — token generated at https://publish.buffer.com/settings/api
BUFFER_TOKEN=
